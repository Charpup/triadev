#!/usr/bin/env python3
"""TriadDev CLI Wrapper"""
import sys
from pathlib import Path

TRIADEV_HOME = Path.home() / ".openclaw" / "skills" / "triadev"
sys.path.insert(0, str(TRIADEV_HOME / "lib"))

from triadev.cli import main

if __name__ == '__main__':
    sys.exit(main())
